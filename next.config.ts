import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // PWA configuration will be handled via manifest.json
  // Service worker can be added via next-pwa if needed
};

export default nextConfig;
